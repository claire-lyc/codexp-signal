# Global disease.sh downloader

Run:

- `python API/diseases/global/clone_global.py`

Pick which API group to download by editing `ACTIVE_GROUP` at the top of:

- `API/diseases/global/clone_global.py`

Downloaded JSON is stored under:

- `data/diseases/global/<group>/`
